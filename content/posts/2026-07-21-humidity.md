---
layout: post
bodyClass: post-page
title: Reading humidity on the Sense HAT
date: '2026-07-21'
slug: humidity
tags:
- Java
- Pi4J
- Sense HAT
- Raspberry Pi
- Sensors
description: Read the relative humidity from the Sense HAT HTS221 sensor and display it as a scrolling message on the LED matrix.
image: posts/humidity.jpg
---


`Humidity` reads the relative humidity from the Sense HAT and scrolls the value across the LED matrix every 5 seconds.

Source code: [projects/Humidity/Humidity.java](https://github.com/igfasouza/pi4j-sensehat-playground/blob/main/projects/Humidity/Humidity.java)

## How it works

The Sense HAT includes an [HTS221](https://www.st.com/en/mems-and-sensors/hts221.html) humidity and temperature sensor over I²C. Pi4J's `SenseHat` driver already talks to it — you can just call `hat.getHumidity()` and get the value in %RH.

The main loop:

1. Reads the current humidity with `hat.getHumidity()`.
2. Rounds it to one decimal place: `Math.round(humidity * 10.0) / 10.0`.
3. Builds a message like `H-42.7%` and calls `hat.showMessage(message, 70, textColor, backColor)` to scroll it across the matrix in green on black.
4. Sleeps 5 seconds and repeats.

Because the LED matrix generates a bit of heat, the reading is close to but not identical to the ambient value — see the [ApproxTemp](/pi4j-sensehat-playground/posts/approx-temp/) example for how to apply a calibration offset.

## Running it

This script depends on a specific branch of `pi4j-drivers` (my `igfasouza` branch), so clone and install it locally first:

```bash
git clone -b igfasouza https://github.com/igfasouza/pi4j-drivers.git
cd pi4j-drivers
mvn install
```

Then run it with [JBang](https://www.jbang.dev/) on a Raspberry Pi with a Sense HAT attached:

```bash
jbang projects/Humidity/Humidity.java
```
